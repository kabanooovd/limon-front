import React from "react"
import styled from "styled-components"

const StylesFooter = styled.div`
  background: blue;
  width: 100%;
  height: 50px;
`;

export const Footer = () => {
  return <StylesFooter>
    sdsd
  </StylesFooter>
}