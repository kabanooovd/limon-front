import React from "react"
import styled from "styled-components"

const StylesHeader = styled.div`
  background: green;
  width: 100%;
  height: 50px;
`;

export const Header = () => {
  return <StylesHeader>
    sdsd
  </StylesHeader>
}